import { StyleSheet } from 'react-native';
import { bdOrange, darkBlue } from '@constants/colors';

export default StyleSheet.create({
  focused: {
    tintColor: bdOrange
  },
  notFocused: {
    tintColor: darkBlue
  }
});
